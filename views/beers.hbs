<div>

    {{#each this.arrBeers}}
        <h3>{{name}}</h3>
        <img src="{{this.image_url}}" alt="beer" height="200px">
        <p>{{tagline}}</p>
        <p>{{description}}</p>
    {{/each}}

</div>